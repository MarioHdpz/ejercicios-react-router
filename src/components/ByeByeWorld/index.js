import React from "react";
/* import { ? } from 'react-router-dom'; */

const ByeByeWorld = props => (
    <div>
        <h1 className="main">Bye bye world!</h1>
    </div>
);

export default ByeByeWorld;
